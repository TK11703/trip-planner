@inherits LayoutComponentBase

@* Chrome-free layout for printable pages: renders only the page body, with no
   navigation, footer, or theme selector, so the printed output contains only content. *@
<main class="tp-print-shell">
    @Body
</main>
