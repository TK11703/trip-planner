# azd keeps per-environment state and secrets here, so the default is to commit nothing.
# accepted-risks.json is the deliberate exception: the readiness gate reads it at a fixed
# path, so it has to exist in a clean CI checkout or every waiver silently stops applying.
*
!.gitignore
!accepted-risks.json
